# RDI 1.20.1 Chunk Cache

This implementation is adapted from the GPLv3-licensed NotEnoughBandwidth
1.20.1 persistent and delayed chunk cache code in `CODEREF`.

The networking and mixin hooks were rewritten for Forge 1.20.1 and RDI's
existing `SimpleChannel`.
